<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.local.cadmeupet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CadêMeuPet"
        tools:ignore="AllowBackup">
        <activity android:name=".ConfigActivity"></activity>
        <activity android:name=".AdotarPetActivity" />
        <activity android:name=".PetPerdidoActivity" />
        <activity android:name=".FazerPublicacaoActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".BuscarActivity" />
        <activity android:name=".SocialActivity" />
        <activity android:name=".RecuperarUsuActivity" />
        <activity android:name=".TermosdeUsoActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".CadastroUsuActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.SemBarra" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.SemBarra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>